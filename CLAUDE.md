# CLAUDE.md

本文件是 Claude Code 在 `bpq`（拓竹 A1 定时静默打印调度器）项目中的工作约束。
项目背景、目录结构、设计约定、已知坑见 [README.md](README.md) 与 [docs/](docs/)，
本文件不重复，只补充"怎么协作"和"哪些红线不能碰"。

## 基础约束

这些是本项目的硬约束，任何实现方案都不能违反（详见 README「设计约定」「已知坑」）：

- **触发前必须完全静默**——项目唯一存在理由，不接受任何让打印机提前有动作的方案。
- **任务必须持久化**——服务重启 / 睡眠唤醒后待发任务要还在（SQLite jobstore）。
- **到点不空闲就放弃，写日志**——不做重试、不做队列。
- **传输层只实现通道 A（本地 MQTT + FTPS）**，通道 B 只占位不实现。
- **改动前先看 docs/**，尤其是 `docs/验证记录-通道A.md` 里的实测结论（比如 FTPS 必须
  加密、A1 只支持被动模式、不回 `close_notify` 等），这些是踩过坑得出的，不要凭社区
  文档或直觉去改。
- **不升级打印机固件**，不引入推送通知 / 多任务多打印机 / 参与切片 / 云端部署。
  （原本还有「不做 Web UI」，**用户于 2026-08-24 明确推翻了这一条**。v0.2 起 WebUI
  是项目的一部分，但它必须寄生在 daemon 进程里、复用同一条 MQTT 长连接，
  且不提供「立即打印」、不做 SD 卡文件管理、不做多打印机。）
- **任何代码路径都不得在 daemon 运行期间另建 MQTT 连接。** 要打印机数据一律走
  `PrinterLink`；在别的进程里要，一律走 daemon 的 HTTP API。这条是「打印机同一时刻
  只接受一个连接」的直接推论，违反它会重现 v0.1 验收时踩过的
  「互抢连接 → 到点读到 UNKNOWN → 打印机明明空闲却被放弃」。
- **「我们发了什么」与「机器说了什么」在数据结构上分开。** 下发参数存在
  `Task.options` / `Task.sent_payload`，上报状态存在 `PrinterSnapshot`，两者不相交，
  界面上也不做对照——五个打印开关里有三个根本没有对应的上报字段。
- **前端产物 `web/dist` 不入库。** 改前端改 `web/src/`，别手改构建产物。
- **不再有「假打印机模式」。** v0.2 期间为了在没接真机时开发 WebUI 而存在，
  v0.3 真机通了之后已从产品代码删除，只在 `tests/fakeprinter.py` 里留给测试用。
  别把它加回 `src/`——对着一台合成的打印机调半天真问题，代价比省下的麻烦大。
- 改动涉及真实打印机动作（真的会开始打印、真的会上传文件）时，明确告知用户这一步
  是否需要人在场，不要在没有确认的情况下让人误以为是纯本地测试。
- **每次发布（打 tag / 发 Release）前必须先在 `CHANGELOG.md` 写清楚这次改了
  什么**，不能只有版本号没有内容——发 Release 前是否已经更新 CHANGELOG，是
  判断"能不能打 tag"的前置检查项之一。Release 现在覆盖四个二进制：Windows
  托盘版 + 控制台版、Linux 控制台版、macOS 控制台版（Apple Silicon），全部是
  本地免装 Python/Node 的可执行文件；改动这个平台矩阵前先看
  `.github/workflows/release.yml` 顶部注释。

## 运行基础需求：多 Agent 协作模式

本项目按下面的角色分工推进任务，不是单个模型从头写到尾：

| 角色 | 模型 | 职责 |
|---|---|---|
| 规划者 / 调度者 | Opus 5（高推理强度） | 理解需求、制定方案、拆分任务、下发给子 agent、汇总验证结果 |
| 编码者 | Sonnet 5（高推理强度） | 按任务卡实现具体代码改动 |
| 测试者 | Haiku | 编写/运行测试，验证编码者的改动是否满足任务卡要求 |

标准流程（当前会话作为规划者/调度者主导）：

1. **理解需求**——把用户的请求转成明确的目标和验收标准，必要时对照「基础约束」检查
   是否触碰红线。
2. **Plan**——产出实现方案，涉及非平凡改动时用 Plan 模式与用户对齐后再动手。
3. **拆 Tasks**——把方案拆成边界清晰、可独立验证的任务卡（每张任务卡说明目标文件、
   改动范围、验收标准）。
4. **下发任务给子 agent**——用 `Agent` 工具派发：
   - 编码类任务 → `model: "sonnet"`，提示词里带上任务卡的目标文件、范围、验收标准，
     不要求子 agent 自己去猜需求。
   - 测试类任务 → `model: "haiku"`，负责跑 `pytest` / 写针对性测试用例，报告通过与否
     和具体失败点。
   - 任务之间有依赖时顺序下发；相互独立时可在同一条消息里并行下发。
5. **验证**——子 agent 返回后，调度者亲自核对改动（读 diff、跑一次关键命令），不要只
   凭子 agent 的自我总结判断是否完成。
6. **返回结构化结果**——每轮任务完成后，给出：改了什么文件、验证方式、结论（通过/不
   通过）。
7. **收尾**：
   - **验证通过** → 提交 git commit（新建 commit，不 amend、不 `--no-verify`），然后在
     对话中向用户简要介绍本次修改的内容和原因。
   - **验证不通过** → 不提交，回到第 3 步继续修改和测试，直到通过或需要用户澄清为止。

## Git 约束

分支怎么开、怎么合、怎么发版、怎么清理，完整规则在
`.claude/skills/git-branch/SKILL.md`，需要新建分支、给子 agent 开 worktree、合并、
打 tag 发版或清理分支时先读它，本节只留常驻硬红线。

- **禁止把自己（Claude / Anthropic）加为协作者**：提交信息中不得包含
  `Co-Authored-By: Claude` 或任何形式的 Claude/Anthropic 署名，这是本项目对 Claude Code
  默认提交模板的覆盖。
- 不修改 `git config`（不设置/覆盖 `user.name`、`user.email` 等）。
- 只在验证通过后创建新 commit；不使用 `--force`、`--no-verify`、`reset --hard` 等破坏性
  操作，除非用户明确要求。
- **commit message 用 Conventional Commits（约定式提交）格式**：`<type>(<scope>): <description>`。
  - `type` 从这几个里选：`feat`（新功能）、`fix`（修 bug）、`docs`（文档，含 README/CLAUDE.md）、
    `refactor`（不改行为的重构）、`test`（测试）、`chore`（杂项/构建/依赖）、`perf`（性能）、
    `style`（纯格式，不改逻辑）。
  - `scope` 可选，写改动涉及的模块/目录，比如 `scheduler`、`web`、`transport`。
  - `description` 依旧用**中文**撰写，聚焦"为什么改"，不用中文以外的语言。
  - 例：`fix(daemon): 修 Windows 上 Ctrl+C 杀不掉进程的 bug`、
    `docs(claude): 补充 Conventional Commits 提交规范`。
