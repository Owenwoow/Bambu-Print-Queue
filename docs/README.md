# 文档索引

## 面向使用者

- [`部署-Docker.md`](部署-Docker.md) —— Docker / docker compose 部署详细教程。
  非 Docker 部署（systemd / 直接跑）见项目根 [README.md](../README.md) 的「部署」一节。

## 面向想改代码的人

- [`验证记录-通道A.md`](验证记录-通道A.md) —— 通道 A（本地 MQTT + FTPS）的真机实测记录：
  FTPS 必须加密、A1 只支持被动模式、不回 `close_notify` 等结论都在这里，
  改传输层前必读，别凭社区文档或直觉改。
- [`v0.2-WebUI-架构.md`](v0.2-WebUI-架构.md) —— WebUI 寄生在 daemon 进程里的架构决策
  （为什么不能另开 MQTT 连接、REST + SSE 的分工）。
- [`v0.2-多色映射语义.md`](v0.2-多色映射语义.md) —— `ams_mapping` 里外部料语义
  （`-1` 还是 `255`）的取证进度，多色打印功能未完成的原因在这里。
- [`v0.3-浅色主题-demo.md`](v0.3-浅色主题-demo.md) —— WebUI 浅色主题实现记录。
- [`定时打印任务调度器-项目框架-v0.1.md`](定时打印任务调度器-项目框架-v0.1.md) ——
  v0.1 最初的项目框架文档，代码注释里仍有引用。

## archive/

开发过程中的可行性调研、阶段性任务清单等一次性记录，内容已被上面的文档或
项目根 README 覆盖，仅作历史参考，不代表当前状态。
