# 贡献指南

bpq 是一个个人向的小项目：拓竹 A1 定时静默打印调度器，解决「睡前忘记点开始打印」
这一个具体痛点。项目范围刻意收得很窄，欢迎 bug 报告，也欢迎符合项目范围的 PR，
但请先看下面的红线部分，避免做了方向不对的工作。

## 提交 PR 前必读：硬性红线

这些是项目的设计约束，不是「暂时没做」，PR 提交前请先确认没有触碰：

- **触发前必须完全静默**——这是项目唯一存在的理由。任何会让打印机在预定时刻之前
  提前有动作（预热、转风扇、发指令探测状态等）的方案，一律不接受。
- **不做云端部署**——项目只做本地局域网调度，不考虑公网/云端场景。
- **不做多任务/多打印机支持**——一次只服务一台打印机，不接受排队多机或并发调度。
- **不参与切片**——bpq 只接收已经切好的 `3mf`/`gcode` 文件，不做切片相关功能。
- **不升级打印机固件**，也**不引入完整的推送通知系统**（推送只留接口占位，不实现）。
- **不做「立即打印」、不做 SD 卡文件管理**——这是 WebUI 的范围限制，不是遗漏。
- **任何代码路径都不能在 daemon 运行期间另建 MQTT 连接**——打印机同一时刻只接受
  一个连接，要打印机数据一律走已有的 `PrinterLink`；在别的进程里要，一律走 daemon
  的 HTTP API。这是本项目最容易踩的坑，违反它会导致「互抢连接 → 到点读到 UNKNOWN
  → 打印机明明空闲却被放弃」。

不确定某个想法是否踩线，欢迎先开 issue 讨论，再动手写代码。

## 本地开发

后端：

```bash
pip install -e ".[dev]"
pytest
ruff check .
mypy
```

前端（`web/` 目录下）：

```bash
npm install
npm run dev
```

## 提交信息格式

使用 [Conventional Commits](https://www.conventionalcommits.org/) 格式：

```
<type>(<scope>): <description>
```

`type` 从以下几种里选：`feat`、`fix`、`docs`、`refactor`、`test`、`chore`、`perf`、
`style`。描述文字请用中文，聚焦「为什么改」而不只是「改了什么」。
